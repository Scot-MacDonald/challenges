import styled from "styled-components";

export const StyledButton = styled.button`
  appearance: none;
  border: border 1px solid;
  background: none;
  font-size: larger;
  padding: 0.5rem 1rem;
  &:hover {
    cursor: pointer;
  }
`;
